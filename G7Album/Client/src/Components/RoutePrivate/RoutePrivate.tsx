import {  } from "react-router";
// import { Route, Redirect, RouteComponentProps } from "react-router-dom";

// interface IRoutePrivate {
//   component:
//     | React.ComponentType<any>
//     | React.ComponentType<RouteComponentProps<any, StaticContext, unknown>>
//     | undefined;
//   path: string;
//   exact?: boolean;
// }

// const RoutePrivate: React.FC<IRoutePrivate> = ({ component, path, exact }) => {
//   const jwt = JSON.parse(localStorage.getItem("jwtActivities") as any);

//   // TODO: Implementar busqueda al back para obtener token y comparar token con el del Front

//   if (jwt) {
//     return <Route path={path} exact={exact} component={component} />;
//   } else {
//     return <Redirect to="/loginA" />;
//   }
// };

// export default RoutePrivate;
